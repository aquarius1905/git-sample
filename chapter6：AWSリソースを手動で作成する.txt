## はじめに

これまでにLambda関数をローカル環境で開発・テストできるように整えてきました。
本章では、**AWSコンソール**を使って実際のAWS環境に必要なリソースを作成し、Lambda関数をデプロイします。

手動でリソースを作成することで、各サービスの設定項目や関係性を理解しながら、
**最短時間で動作する環境**を構築できます。


## 作成するリソース

今回作成するAWSリソースは以下の3つです。

| 作成順序 | リソース | 目的 |
|---------|------|----------|
| 1 | **S3バケット** | CSVファイルの保存先 |
| 2 | **SQSキュー** | JSONメッセージの受信 |
| 3 | **Lambda関数** | ETL処理の実行 |


## ステップ1：S3バケットの作成

### S3コンソールでバケットを作成

1. **AWSコンソール**にログインし、**S3**サービスを開く
2. 「**バケットを作成**」をクリック

![](https://storage.googleapis.com/zenn-user-upload/d1e0a8396e9e-20250923.jpeg)

3. 以下の設定を入力する

**基本設定**
- **バケットタイプ**: **汎用**を選択
- **バケット名**: `lambda-etl-bucket-[あなたの名前]-[日付]`
  - 例：`lambda-etl-bucket-fd2025-20250924`
  - バケット名は全世界でユニークである必要があります
- **AWSリージョン**: `アジアパシフィック（東京）ap-northeast-1`

**オブジェクト所有権**
- **ACL無効（推奨）** を選択

![](https://storage.googleapis.com/zenn-user-upload/ca1334930ce7-20250923.jpg)


**パブリックアクセス設定**
- **パブリックアクセスをすべてブロック**にチェック（デフォルト）

**バケットのバージョニング**
- **無効**を選択

![](https://storage.googleapis.com/zenn-user-upload/7836f0cbc407-20250923.jpeg)

**デフォルト暗号化**
- **デフォルト設定**のまま

4. 「**バケットを作成**」をクリック

![](https://storage.googleapis.com/zenn-user-upload/fc417525cae3-20250923.jpeg)

### 作成確認

バケット一覧に作成したバケットが表示されることを確認してください。

![](https://storage.googleapis.com/zenn-user-upload/9451757a148e-20250923.jpeg)

## ステップ2：SQSキューの作成

### 2-1. メインキューの作成

1. **SQS**サービスを開く
2. 「**キューの作成**」をクリック

![](https://storage.googleapis.com/zenn-user-upload/d98a5aec1dc3-20250925.jpeg)

3. 以下の設定を入力する

**基本設定**
- **タイプ**: **標準**を選択
- **名前**: `lambda-etl-queue`

![](https://storage.googleapis.com/zenn-user-upload/535ba83ad5a4-20250925.jpeg)

**設定**
- **可視性タイムアウト**: `300秒`（5分）
  - Lambda関数のタイムアウト時間より長く設定
- **メッセージ保持期間**: `14日`
- **その他の項目**: デフォルト値

![](https://storage.googleapis.com/zenn-user-upload/c160b7b12794-20250925.jpeg)

**その他の設定**
- デフォルトのまま

4. 「**キューの作成**」をクリック

![](https://storage.googleapis.com/zenn-user-upload/49fa18ca3f49-20250925.jpeg)

### 2-2. デッドレターキューの作成

1. 再度「**キューの作成**」をクリック
![](https://storage.googleapis.com/zenn-user-upload/41afcfe074fa-20250925.jpeg)

2. 以下の設定を入力：

**基本設定**
- **タイプ**: **標準**を選択
- **名前**: `lambda-etl-dlq`

![](https://storage.googleapis.com/zenn-user-upload/3bc8e75b353a-20250925.jpeg)

**その他の設定**
- デフォルトのまま

3. 「**キューの作成**」をクリック

![](https://storage.googleapis.com/zenn-user-upload/49fa18ca3f49-20250925.jpeg)

### 2-3. メインキューにデッドレターキューを設定

1. 作成した `lambda-etl-queue` をクリックして詳細画面を開く
2. 「**編集**」ボタンをクリック

![](https://storage.googleapis.com/zenn-user-upload/a5fa8d6e2a27-20250925.jpeg)

3. **デッドレターキュー**セクションで以下を設定

**デッドレターキュー**
- **有効**にチェック
- **最大受信回数**: `3`
- **キューの選択**: `lambda-etl-dlq` を選択

4. 「**保存**」をクリック

![](https://storage.googleapis.com/zenn-user-upload/351050f41459-20250925.jpeg)

![](https://storage.googleapis.com/zenn-user-upload/30b7b355de51-20250925.jpeg)

### 2-4. 作成確認

キュー一覧に以下の2つのキューが作成され、メインキューにDLQが設定されていることを確認する
- `lambda-etl-queue`（メインキュー、DLQ設定済み）
- `lambda-etl-dlq`（デッドレターキュー）

![](https://storage.googleapis.com/zenn-user-upload/670d066f69f3-20250925.jpeg)

![](https://storage.googleapis.com/zenn-user-upload/f6f98e4304f7-20250925.jpeg)

## ステップ3：Lambda関数の作成

### Lambdaコンソールで関数を作成

1. **Lambda**サービスを開く
2. 「**関数の作成**」をクリック

![](https://storage.googleapis.com/zenn-user-upload/c1aa2300f1c5-20250927.jpg)

3. 「**一から作成**」を選択
4. 以下の設定を入力する

**基本的な情報**
- **関数名**: `lambda-etl-processor`
- **ランタイム**: `Python 3.12`
- **アーキテクチャ**: `x86_64`

![](https://storage.googleapis.com/zenn-user-upload/ded974624076-20250927.jpg)


**実行ロール**
- **基本的なLambda権限で新しいロールを作成**を選択

5. 「**関数の作成**」をクリック

![](https://storage.googleapis.com/zenn-user-upload/2b5823c136b2-20250929.jpeg)

### 関数コードのデプロイ

#### デプロイパッケージの作成

ローカルで以下のコマンドを実行し、デプロイ用のzipファイルを作成します。

```bash
# プロジェクトルートディレクトリで実行
cd lambda-etl

# デプロイ用ディレクトリを作成（既存があれば削除）
rm -rf deploy-package
mkdir deploy-package

# __pycache__を除外してコピー
rsync -av --exclude='__pycache__' --exclude='*.pyc' app/ deploy-package/

# zipファイルを作成
cd deploy-package
zip -r ../lambda-etl-function.zip .
cd ..
```

#### zipファイルのアップロード

1. Lambda関数の**コード**タブを開く
2. 「**アップロード元**」→「**.zipファイル**」をクリック

![](https://storage.googleapis.com/zenn-user-upload/1e6ffac5cfd9-20250930.jpg)

3. 作成した `lambda-etl-function.zip` を選択してアップロード

![](https://storage.googleapis.com/zenn-user-upload/a1890203676a-20250930.jpg)

4. 「**保存**」をクリック

![](https://storage.googleapis.com/zenn-user-upload/c6ec0a210a18-20250930.jpg)


#### ハンドラーの設定

**設定**タブの**全般設定**で：
- **ハンドラー**: `main.lambda_handler` に変更
- **タイムアウト**: `3分`
- **メモリ**: `256MB`


## ステップ4：IAM権限の設定

Lambda関数がS3とSQSにアクセスできるよう権限を追加します。

### 実行ロールへのポリシー追加

1. Lambda関数の**設定**タブ→**権限**を開く
2. **実行ロール**のリンクをクリック（IAMコンソールが開く）
3. 「**許可を追加**」→「**ポリシーをアタッチ**」をクリック
4. 以下のポリシーを検索して追加：
   - `AmazonS3FullAccess`
   - `AmazonSQSFullAccess`

注意：本番環境では最小権限の原則に従い、より制限的なポリシーを使用してください。

## ステップ5：環境変数の設定

1. Lambda関数の**設定**タブ→**環境変数**を開く
2. 以下の環境変数を追加：

| キー | 値 | 説明 |
|-----|---|------|
| `S3_BUCKET_NAME` | `lambda-etl-bucket-[あなたの名前]-[日付]` | 作成したS3バケット名 |
| `KEY_PREFIX` | `etl-output` | S3内のファイル保存先プレフィックス |

3. 「**保存**」をクリック

## ステップ6：SQSトリガーの設定

Lambda関数にSQSキューからのイベントを処理するトリガーを設定します。

### トリガーの追加

1. Lambda関数の**設定**タブ→**トリガー**を開く
2. 「**トリガーを追加**」をクリック
3. 以下の設定を入力：

**トリガーの設定**
- **ソース**: `SQS`
- **SQSキュー**: 作成した `lambda-etl-queue` を選択
- **バッチサイズ**: `1`（1メッセージずつ処理）
- **有効**にチェック

4. 「**追加**」をクリック

## ステップ7：作成したリソースの確認

### AWS CLIでの確認（オプション）

```bash
# S3バケットの確認
aws s3 ls | grep lambda-etl

# SQSキューの確認
aws sqs list-queues | grep lambda-etl

# Lambda関数の確認
aws lambda list-functions --query 'Functions[?contains(FunctionName, `lambda-etl`)]'
```

### AWSコンソールでの最終確認

1. **S3**: バケットが作成されている
2. **SQS**: メインキューとDLQが作成されている
3. **Lambda**: 関数が作成され、SQSトリガーが設定されている
4. **IAM**: Lambda実行ロールにS3・SQS権限が付与されている


## 作成完了

これで、ETL処理に必要なAWSリソースの作成が完了しました。

**作成されたリソース一覧：**
- S3バケット：CSVファイルの出力先
- SQSキュー：JSONメッセージの受信
- Lambda関数：ETL処理ロジック
- IAMロール：必要な権限設定

## トラブルシューティング

### よくあるエラーと対処法

**1. バケット名が重複**
```
BucketAlreadyExists: The requested bucket name is not available
```
対処：バケット名に追加の識別子（日付、名前など）を付けて再作成

**2. 権限エラー**
```
AccessDenied
```
対処：IAMロールにS3FullAccess、SQSFullAccessが正しく付与されているか確認

**3. Lambda関数のインポートエラー**
```
Unable to import module 'main'
```
対処：zipファイルの構造を確認。main.pyがルートディレクトリにあることを確認

## リソースの削除

学習完了後、料金を避けるためリソースを削除する場合：

1. **Lambda関数**：削除
2. **SQSキュー**：両方削除（メインキュー、DLQ）
3. **S3バケット**：オブジェクトを削除してからバケット削除
4. **IAMロール**：Lambda関数削除時に自動削除される

## まとめ

手動作成により、以下のメリットがあります。
- AWSサービスの設定項目を理解できる
- サービス間の関係性を把握できる
- 短時間（15-20分）で動作環境を構築できる

## 次章の内容

実際にSQSにメッセージを送信し、Lambda関数が動作してS3にCSVファイルが作成されることを確認します。
また、CloudWatchでのログ監視や基本的なトラブルシューティング方法も解説します。